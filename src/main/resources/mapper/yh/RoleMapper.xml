<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.back.yh.mapper.RoleMapper" >
    <resultMap id="RoleResultMap" type="com.back.yh.pojo.Role" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="role_id" property="roleId" jdbcType="INTEGER" />
        <result column="role_name" property="roleName" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <collection property="userList" ofType="String">
            <result column="user_name"></result>
        </collection>
        <collection property="menuList" ofType="String">
            <result column="menu_name"></result>
        </collection>
    </resultMap>

    <resultMap id="MenuResultMap" type="com.back.yh.pojo.Menu" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="menu_id" property="menuId" jdbcType="INTEGER" />
        <result column="menu_name" property="menuName" jdbcType="VARCHAR" />
    </resultMap>
    <resultMap id="UserResultMap" type="com.back.yh.pojo.User" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="user_id" property="userId" jdbcType="INTEGER" />
        <result column="user_name" property="userName" jdbcType="VARCHAR" />
    </resultMap>
    <resultMap id="RoleResultMap2" type="com.back.yh.pojo.Role" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="role_id" property="roleId" jdbcType="INTEGER" />
        <result column="role_name" property="roleName" jdbcType="VARCHAR" />
    </resultMap>

    <select id="getTotalRole" resultType="Integer">
        select count(*) from role where status=1
    </select>

    <select id="initRolePage" resultMap="RoleResultMap" parameterType="com.back.yh.pojo.Role">
        select r.*,u.user_id,u.user_name,m.menu_id,m.menu_name from role r left join user_role ur
        on r.role_id=ur.role_id and ur.`status`=1
        LEFT JOIN user u on u.user_id=ur.user_id and u.`status`=1
        LEFT JOIN role_menu rm on rm.role_id=r.role_id and rm.`status`=1
        LEFT JOIN menu m on m.menu_id=rm.menu_id and m.`status`=1
        where r.`status`=1
    </select>

    <select id="getMenuListForRole" resultMap="MenuResultMap">
        select * from menu where status=1
    </select>
    <select id="getUserListForRole" resultMap="UserResultMap">
        select * from user where status=1
    </select>

    <insert id="addRole" parameterType="String">
        insert into role (role_name) values (#{roleName})
    </insert>

    <select id="getRoleId" parameterType="String" resultType="Integer">
        select role_id from role where status = 1 and role_name=#{roleName}
    </select>

    <insert id="addRoleMenu" parameterType="com.back.yh.dto.RoleMenu">
        insert into role_menu (role_id,menu_id) values (#{roleId},#{menuId})
    </insert>
    <insert id="addRoleUser" parameterType="com.back.yh.dto.RoleUser">
        insert into user_role (role_id,user_id) values (#{roleId},#{userId})
    </insert>

    <select id="getMenuByRoleId" resultMap="MenuResultMap" parameterType="int">
        SELECT m.menu_id,m.menu_name from role_menu rm LEFT JOIN menu m on rm.menu_id=m.menu_id
         and m.`status`=1
        where rm.role_id = #{roleId} and rm.`status`=1
    </select>
    <select id="getUserByRoleId" resultMap="UserResultMap" parameterType="int">
        SELECT u.user_id,u.user_name from user_role ur LEFT JOIN user u on ur.user_id=u.user_id
         and u.`status`=1
        where ur.role_id = #{roleId} and ur.`status`=1
    </select>
    <select id="getRoleByRoleId" resultMap="RoleResultMap2" parameterType="int">
        SELECT role.role_name from role
        where role_id = #{roleId}
    </select>

    <update id="updateRole" parameterType="com.back.yh.vo.RoleVo">
        update role set role_name=#{roleName},update_time=#{updateTime} where role_id = #{roleId}
    </update>
    <update id="deleteRoleMenu" parameterType="int">
        update role_menu set status=0 where role_id = #{roleId}
    </update>
    <update id="deleteUserRole" parameterType="int">
        update user_role set status=0 where role_id = #{roleId}
    </update>
    <update id="deleteRole" parameterType="int">
        update role set status=0 where role_id = #{roleId}
    </update>

</mapper>